# Frontend Mock Archive

`frontend-mock/` is archived reference material.

It is not the active product frontend and should not be treated as the implementation target for new work.

Use it only for:

- visual reference
- discarded experiments
- design comparison while rebuilding the production frontend in `frontend/`

Canonical product work belongs in `frontend/`.
