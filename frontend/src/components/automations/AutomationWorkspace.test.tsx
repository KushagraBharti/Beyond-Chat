import { fireEvent, render, screen, waitFor } from "@testing-library/react";
import { describe, expect, it } from "vitest";
import { AutomationWorkspace } from "./AutomationWorkspace";
import type { AutomationDashboardData } from "../../features/automations";
const data:AutomationDashboardData={automations:[{id:"aut_1",name:"Weekly finance review",state:"paused",trigger:"Monday 08:30 America/Chicago",version:"v4 · sha256:31b7",owner:"Finance Ops",principal:"svc-finance",failureCount:1,costCents:42,costLimitCents:500}],runs:[{id:"run_1",state:"dead_letter",trigger:"Schedule",version:"v4",startedAt:"Jul 7, 08:30",attempt:3,costCents:42,detail:"Destination rejected after bounded retries."}],approvals:[{id:"apr_1",automationName:"Weekly finance review",action:"Send email to leadership list",expiresAt:"Jul 12, 09:00",status:"pending"}],integrations:{persistence:"in_memory",runtime:"simulated",scheduler:"not_connected",composio:"not_connected",notifications:"in_memory"}};
describe("AutomationWorkspace",()=>{it("labels reference adapters and exposes failure and approval inboxes",async()=>{render(<AutomationWorkspace initialData={data}/>);expect(await screen.findByText("Reference adapters only")).toBeInTheDocument();fireEvent.click(screen.getByRole("button",{name:/failures/i}));expect(await screen.findByText("Destination rejected after bounded retries.")).toBeInTheDocument();fireEvent.click(screen.getByRole("button",{name:/approvals/i}));expect(await screen.findByText("Send email to leadership list")).toBeInTheDocument();fireEvent.click(screen.getByRole("button",{name:"Approve once"}));await waitFor(()=>expect(screen.getByText("No pending approvals")).toBeInTheDocument());});});
