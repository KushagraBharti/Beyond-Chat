import { useNavigate } from "react-router-dom";
import { PageHeader } from "../../components/workspace/WorkspacePrimitives";

const migration: Readonly<Record<string, { readonly target: string; readonly action: string; readonly detail: string }>> = {
  writing: { target: "/chat", action: "/document", detail: "Writing is a Research capability and document output template." }, research: { target: "/chat", action: "/research", detail: "Research becomes a cited Work task or output, rather than a standalone studio." }, image: { target: "/chat", action: "/image", detail: "Images are requested outputs inside Work and Projects." }, data: { target: "/work", action: "/spreadsheet", detail: "Safe analysis belongs to General; finance-specific analysis belongs to Finance." }, finance: { target: "/work", action: "@Finance", detail: "Finance retains its legacy adapter boundary until Pi parity is proven." }, artifacts: { target: "/projects", action: "", detail: "Artifacts remain discoverable as outputs in Work, Projects, search, and recents." },
};
export function LegacyMigrationPage({ legacy }: { legacy: string }) { const navigate = useNavigate(); const item = migration[legacy]; return <section className="workspace-page"><PageHeader eyebrow="Compatibility route" title={`${legacy} has moved into the workspace.`} /><section className="workspace-panel workspace-migration"><p>{item.detail}</p><p>{item.action ? `Open the canonical surface and use ${item.action} to form a visible typed request. Promote to Work when progress, tools, approvals, or outputs must persist.` : "Find prior work as outputs inside a project or task."}</p><button className="workspace-button" type="button" onClick={() => navigate(item.target)}>Continue to {item.target === "/chat" ? "Chat" : item.target === "/work" ? "Work" : "Projects"}</button></section></section>; }
