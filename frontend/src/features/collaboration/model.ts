export interface CollaboratorView { readonly id: string; readonly name: string; readonly initials: string; readonly state: "active" | "idle" | "offline"; readonly location: string; }
export interface CommentView { readonly id: string; readonly author: string; readonly body: string; readonly createdAt: string; readonly anchorLabel: string; readonly resolved: boolean; readonly replies: readonly CommentView[]; }
export interface ReviewView { readonly id: string; readonly reviewer: string; readonly status: "pending" | "approved" | "changes_requested"; readonly note: string | null; readonly canDecide?: boolean; }
export interface RunProgressView { readonly runId: string; readonly label: string; readonly state: "queued" | "running" | "waiting" | "complete" | "failed"; readonly completedSteps: number; readonly totalSteps: number; }
export interface CollaborationActions { readonly onAddComment: (body: string) => void | Promise<void>; readonly onMention: (userId: string) => void; readonly onResolve: (commentId: string) => void | Promise<void>; readonly onRequestReview: () => void | Promise<void>; readonly onDecideReview?: (reviewId: string, decision: "approved" | "changes_requested") => void | Promise<void>; }
