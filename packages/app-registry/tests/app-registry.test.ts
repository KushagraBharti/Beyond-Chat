import test from "node:test"; import assert from "node:assert/strict"; import { HIGH_VALUE_APPS, applyConnectReturn, assertAppManifest, createConnectLinkRequest, transitionConnection } from "../src/index.ts";
test("Connect Link exposes no credential and revocation removes account reference", () => { const app = HIGH_VALUE_APPS[0]!; const request = createConnectLinkRequest(app, "user", "user.a", ["files.search"]); assert.deepEqual(request.session_tool_allowlist, ["files.search"]); const c = applyConnectReturn(request, { request_id: request.request_id, status: "connected", connected_account_reference: "opaque" }, "user.a", "2026-01-01"); assert.equal(c.status, "connected"); const revoked = transitionConnection(c, "revoke", "user.a", "2026-01-02"); assert.equal(revoked.connected_account_reference, undefined); assert.equal(revoked.status, "revoked"); });
test("invalid action never enters a Connect Link session", () => assert.throws(() => createConnectLinkRequest(HIGH_VALUE_APPS[0]!, "user", "u", ["admin"])));
test("app manifests require pinned unique actions", () => assert.throws(() => assertAppManifest({ ...HIGH_VALUE_APPS[0]!, actions: [] })));
