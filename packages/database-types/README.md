# `@beyond/database-types`

Deterministic Supabase types for the canonical Beyond Chat data plane.

Regenerate after an applied migration:

```powershell
supabase gen types typescript --linked
```

The generated output belongs in `src/database.types.ts`. Review the schema
diff before replacing the checked-in file.
