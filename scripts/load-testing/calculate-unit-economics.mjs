import {readFile} from "node:fs/promises";const input=JSON.parse(await readFile(process.argv[2]??new URL("../../fixtures/phase12/unit-economics-sensitivity.json",import.meta.url),"utf8"));const results=input.scenarios.map(s=>{const activeSeats=Math.round(s.seats*s.activation_ratio),runs=activeSeats*s.runs_per_active_seat,accepted=runs*s.acceptance_rate,providerCogs=s.fixed_cogs_usd+runs*s.variable_run_cogs_usd,revenue=s.seats*input.seat_price_usd,paymentFees=s.seats*(input.seat_price_usd*.029+.3);return {...s,active_seats:activeSeats,runs,accepted_outputs:accepted,cogs_per_run_usd:providerCogs/runs,cogs_per_accepted_output_usd:providerCogs/accepted,cogs_per_active_seat_usd:providerCogs/activeSeats,organization_provider_cogs_usd:providerCogs,gross_margin_rate:(revenue-providerCogs-paymentFees)/revenue,provider_cogs_share:providerCogs/revenue,guardrail_pass:providerCogs/revenue<=.3};});console.log(JSON.stringify({planning_only:true,external_gates:input.external_gates,results},null,2));
